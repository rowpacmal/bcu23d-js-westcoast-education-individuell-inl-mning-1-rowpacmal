import HttpClient from './http.js';

/* const addData = await new HttpClient().add('courses', {
  name: 'New Name',
}); */

// const removeData = await new HttpClient().remove('courses/18fb');

/* const updateData = await new HttpClient().update('courses/96b6', {
  name: 'New Name',
  text: 'New Text',
}); */

/* const getData = await new HttpClient().get('students');
console.log(getData); */
