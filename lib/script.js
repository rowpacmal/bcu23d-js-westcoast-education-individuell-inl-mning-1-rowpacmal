import HttpClient from './http.js';

/* const addData = await new HttpClient().add('courses', {
  id: 'HejAlma',
  name: 'New Name',
}); */

// const removeData = await new HttpClient().remove('courses/18fb');

/* const updateData = await new HttpClient().update('courses/HejAlma', {
  name: 'New Name',
  text: 'New Text',
});

const getData = await new HttpClient().get('courses');

const removeAllData = () => {
  getData.forEach(async (e) => {
    await new HttpClient().remove(`courses/${e.id}`);
  });
};

removeAllData();

console.log(getData); */
